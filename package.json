{
  "name": "stfc-visual-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Visual intelligence platform for Star Trek Fleet Command",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "echo 'Build script placeholder'",
    "test": "echo 'Test script placeholder'",
    "demo:augur": "ts-node --project tsconfig.demo.json demos/augur-firing-pattern.ts",
    "demo:visual": "ts-node --project tsconfig.demo.json demos/augur-visual-events.ts",
    "demo:validation": "ts-node --project tsconfig.demo.json demos/domain-validation.ts",
    "demo:two-ship": "ts-node --project tsconfig.demo.json demos/two-ship-combat.ts"
    ,"demo:catalog": "ts-node --project tsconfig.demo.json demos/catalog-validation.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0",
    "tsconfig-paths": "^4.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
